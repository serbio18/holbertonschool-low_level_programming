#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer.
 * @n: an integer
 * Return: the absolute number
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (-n);
	}
}

