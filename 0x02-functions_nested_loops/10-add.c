#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * add - a function that adds two integers and returns the result.
 * @n: first integer
 * @m: second integer
 * Return: sum of integers n and m
 */
int add(int n, int m)
{
	int r = 0;

	r = n + m;
	return (r);
}
