#include <stdio.h>

/**
 *  * main - prints the alphabet in lowercase,
 *   * followed by a new line, except q and e
 *    * Return: Aways 0 (Sucess)
 *     */
int main(void)
{
	int ch = 0;

	while (ch < 10)
	{
	printf("%d", ch);
	ch++;
	}
	printf("\n");
	return (0);
}
