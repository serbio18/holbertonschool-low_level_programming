#include <stdio.h>

/**
 * main - prints random number: positive, negative or zero
 * Rerturns: Always 0
 */
int main(void)
{
       	char ch;

	for(ch = 'a' ; ch <= 'z' ; ch++)
       	{
        putchar(ch);
	}
	putchar('\n');
        
	return (0);
}
